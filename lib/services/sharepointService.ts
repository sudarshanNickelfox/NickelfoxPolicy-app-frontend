// SharePoint integration has been removed. Policy documents are now served
// via signed URLs from Supabase Storage through the backend API.
// See: GET /api/policies/:id -> { signed_url: string | null }
export {};
